/*
#include "TextifyConfig.h"

namespace odtr {

Config::Config(const Document::Origin origin)
    : floorBaseline(origin == Document::Origin::SKETCH && false), // this *might* appear in some designs
      limitJustifySpaceWidth(origin == Document::Origin::SKETCH),
      justifyAmbiguous(origin != Document::Origin::SKETCH),
      cutLastLine(origin == Document::Origin::XD),
      shiftBaselineToMiddle(origin == Document::Origin::SKETCH),
      disregardFirstBearing(origin == Document::Origin::PHOTOSHOP),
      disregardLastSpacing(origin == Document::Origin::XD),
      enableRtl(origin != Document::Origin::ILLUSTRATOR),
      allowTrueTypeKerning(origin != Document::Origin::FIGMA),
      forceTrueTypeKerning(origin == Document::Origin::XD),
      infiniteVerticalStretch(origin == Document::Origin::FIGMA),
      lastLineDescenderOffset(origin != Document::Origin::SKETCH),
      preferRealLineHeightOverExplicit(origin == Document::Origin::XD),
      exportOutlines(false),
      considerStyleEncoding(origin != Document::Origin::ILLUSTRATOR),
      internalDisableHinting(true)
{
}

} // namespace odtr
*/
